

// condition ? instruction si true : instruction si false

let age = 15;

let voteable = age < 18 ? 'too young' : 'old enough';

console.log(voteable);


// sunny : '☀️',
// cloudy : '☁️',
// snowy : '❄️',
// rainy : '🌧️',
// '❓'

let weather = 'sunny';

let weatherIcon = weather === 'sunny' ? '☀️' :
                    weather === 'cloudy' ? '☁️' : 
                    weather === 'snowy' ? '❄️' : 
                    weather === 'rainy' ? '🌧️' : '❓';




















// sfbgfnhnghj,hf